require 'spec_helper'

describe Cloudcoverage do
  it 'has a version number' do
    expect(Cloudcoverage::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
